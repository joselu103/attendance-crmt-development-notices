# Documentation Verification

Use proportionate checks: UTF-8 readability, final newlines, relative-link
resolution, and JSON parsing for coordination state. Do not add a test framework
solely to assert prose.

Only after authorized publication, anonymously retrieve declared public URLs.
Record unresolved external owners and approvals in `AGENT_STATE.json`; do not
imply they are complete.
